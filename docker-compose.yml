services:
  db:
    image: postgres:16
    container_name: otec_db
    restart: always
    environment:
      POSTGRES_DB: otec
      POSTGRES_USER: otec_user
      POSTGRES_PASSWORD: otec_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: